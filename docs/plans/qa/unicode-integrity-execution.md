# Registro de execucao: integridade Unicode

Inicio: 2026-09-25. Plano: `docs\plans\2026-09-25-unicode-integrity-fix-plan.md` (nesta worktree). Handover: `docs\plans\2026-09-25-unicode-integrity-handover.md`.

## Gate zero (plano 1.1) — PASS com isolamento

Verificacao read-only executada antes de qualquer mutacao:

- `C:\Users\Marquinho\.config\opencode\opencode.json`, campo `plugin` (projecao segura, somente esta chave): contem `"D:\git\opencode-anthropic-fix"` como path direto. O host vivo carrega o checkout CANONICO do plugin.
- Junction `...\opencode\node_modules\opencode-anthropic-fix`: inexistente. Carregamento e por path direto, nao por junction.
- `...\opencode\node_modules\@tormentalabs\claude-code-wire-compat`: inexistente nesse nivel; a dependencia resolve dentro de `D:\git\opencode-anthropic-fix\node_modules` (registry `0.5.0`, nao link local).
- Outros plugins vivos (`D:\git\opencode-model-router`, `D:\git\opencode-telegram-plugin`): sem dependencia `file:`/`link:` para wire-compat.
- Biblioteca canonica: resolucao completa pelo host UNVERIFIED. P1 executa somente na worktree isolada `D:\git\claude-code-wire-compat-unicode`; canonico somente leitura.
- Sessao atual provavelmente executa atraves do plugin vivo (evidencia de sanitizacao de prompt). Nenhuma mutacao de runtime/install/build/commit no checkout canonico do plugin.

Decisao: E-CAND isolado. Sem consentimento para WIP vivo; sem alteracao de junction/config vivos.

## Adendo de mapeamento canonico -> isolado (vinculante)

| Canonico (default do plano)                                                              | Efetivo nesta execucao                                                                              |
| ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `D:\git\opencode-anthropic-fix` (E-CAND, CWDs de gates, owners, novos arquivos de teste) | `D:\git\opencode-anthropic-fix-unicode`                                                             |
| `D:\git\opencode-anthropic-fix\docs\plans\qa\unicode-integrity-execution.md`             | `D:\git\opencode-anthropic-fix-unicode\docs\plans\qa\unicode-integrity-execution.md` (este arquivo) |
| `D:\git\claude-code-wire-compat` (P1, biblioteca)                                        | `D:\git\claude-code-wire-compat-unicode` (branch `feat/unicode-body-prose`, base `9c9dd6f`)         |

- E-CAND: worktree git `D:\git\opencode-anthropic-fix-unicode`, branch `fix/unicode-integrity`, base `57c9583` (master HEAD na criacao).
- E-BASE: referencia congelada = checkout canonico `D:\git\opencode-anthropic-fix` em `57c9583`, somente leitura; nenhum write/commit la.
- Transferencia de documentos untracked: plano e handover copiados do canonico para E-CAND com SHA256 identicos (verificado via Get-FileHash em 2026-09-25). Originais preservados no canonico, untracked, sem modificacao.

## Baseline registrada

- Plugin canonico: branch `master`, HEAD `57c9583`, versao `1.0.0`, specifier da dependencia `latest`, lock/install `wire-compat 0.5.0`.
- Biblioteca: branch `port/claude-code-2.1.280`, HEAD `9c9dd6f`, versao `0.6.0`. Worktrees existentes: `-release` (`release/0.6.0`), `-tool-choice`.
- Runtimes: Node `v24.15.0`, npm `12.0.2`. Biblioteca exige Node >=20: OK.
- `Intl.Segmenter`: verificar na primeira tarefa de implementacao P2 (Node 24 tem ICU completo por default; registrar confirmacao).

## Leases ativos

| Recurso                                                     | Modo                                             | Dono                            |
| ----------------------------------------------------------- | ------------------------------------------------ | ------------------------------- |
| `D:\git\opencode-anthropic-fix-unicode` (worktree inteira)  | WRITE serializado                                | orquestrador (ate delegacao P2) |
| `D:\git\claude-code-wire-compat-unicode` (worktree inteira) | WRITE unico por vez (regra AGENTS da biblioteca) | orquestrador                    |
| `D:\git\claude-code-wire-compat` (canonico)                 | READ apenas                                      | sem WRITE                       |
| `D:\git\opencode-anthropic-fix` (canonico)                  | READ apenas                                      | qualquer agente, sem WRITE      |

## Autorizacoes PENDENTE (plano 1.2)

- Publicacao RC `@tormentalabs/claude-code-wire-compat@0.7.0-rc.1` fora de `latest` (P2.T0): repo/branch de push, tag, workflow trigger e dist-tag exigem autorizacoes especificas. Candidato somente local; publicacao nao autorizada.
- Probe live sintetico para politica remota de controles de corpo (endpoint/corpus/budget): necessario somente se a evidencia first-party em P1.T1 for insuficiente.
- Publicacao estavel `X.Y.Z` (P4.T7), adocao no host real, eventuais edicoes de host/renderer.
- Alteracao minima de CI (`publish.yml`) somente se o gate de identidade de RC estiver ausente — verificar read-only no pre-flight P1.

## Log de fases

Estado atual prevalente: implementacao de P1 e gates tecnicos verdes no commit `162a02f`, mas fechamento independente de QA BLOCKED por falha operacional (detalhes abaixo). P1 NAO concluida. Causa do incidente original e dos erros de dispatch UNVERIFIED; reproducoes sinteticas nao identificaram o campo original. A permissividade de model/tool-name registrada no historico foi uma regressao ja corrigida, nao uma decisao aprovada. A emenda que dispensava comparacao PRE-publicacao foi retirada: plano 4.1/P2.T0 voltam ao gate aprovado; publicacao continua sem autorizacao. O pack historico de 140 arquivos esta obsoleto, substituido pelo artefato de 145 arquivos abaixo.

### Checkpoint de remediacao P1 (2026-09-25)

- Worktree biblioteca: `D:\git\claude-code-wire-compat-unicode`, branch `feat/unicode-body-prose`, commit assinado local `162a02f` (`fix: close P1 Unicode validation and diagnostic review findings`). Inclui `src/limits.ts` rastreado. Git status limpo depois do pack; nenhum push/publish/tag.
- Corrigidos: stack mutable dos walkers e chaves symbol; paths numericos somente para indices; subarvores dinamicas/desconhecidas mascaradas; truncamento por segmento; raiz valida; offsets/length limitados e codeUnit correlacionado ao reason; controles em identificadores e tokens opacos; validacao de JSON decodificado no parser; governanca e proveniencia.
- Gates executados no codigo do commit, todos exit 0: `npm run typecheck`, `npm run lint`, `npm test` (118 arquivos, 3510 testes), `npm run build`, `npm run format:check`, `npm run test:coverage`, `npm run fixtures:check`, `npm run pack:check`, `npm run test:pack`. Cobertura: 99.06% statements, 98.78% branches, 100% functions, 99.73% lines. Oito fixtures selados; digests congelados identicos em node/bun/workerd para 2.1.195, 2.1.233 e 2.1.280/default.
- QA heavy independente `ses_f250cecb1ffewpyRFO0cI0FuVZ` revisou F1-F11 e R1-R6 e retornou PASS de codigo, condicionado a commitar limits e regenerar o artefato. O verificador automatico nao aceitou o fechamento devido a essas condicoes e lacunas de leitura. As condicoes de commit/pack foram atendidas, mas a chamada final ao mesmo reviewer falhou com `INPUT_TOO_LARGE (maximumSize=1000000)`; uma recuperacao em nova sessao heavy `ses_f24dac121ffeUxGiB4vbwy3xB5` falhou com `INVALID_UNICODE`. Sem autoaprovacao nem downgrade: gate final de P1 BLOCKED.
- RT1 (hipotese de build aceitar ESC170K e parse rejeitar) foi retirado explicitamente pelo reviewer apos evidencia: o builder ja limita body serializado E request normalizado na validacao da evidencia. Probes offline: ESC170K e aspas990K falham no BUILD com INPUT_TOO_LARGE; maiores inputs aceitos nos probes (ESC141921, aspas331149, ASCII496724) passam no parse. Regressoes permanentes cobrem proximidade do limite e rejeicao no build. Nenhum limite foi ampliado.
- Novo candidato local `0.7.0-rc.1`: `C:\Users\MARQUI~1\AppData\Local\Temp\opencode\tormentalabs-claude-code-wire-compat-0.7.0-rc.1.tgz`, 268730 bytes, 145 arquivos, gerado por `npm pack --json --pack-destination` a partir da arvore commitada. Integrity: `sha512-6YWK2hWJQV60bgqvdeWlGx5lewaWxwlvMSwp7sM79KAYCMv6Au6+HYyBVIXhwGp8QjUM5syyFmWSUwk14i8FLg==`.
- Manifesto de conteudo: `C:\Users\MARQUI~1\AppData\Local\Temp\opencode\unicode-162a02f-manifest.json`, SHA256 `4080ad7dc175754825c293e43340a66fa73b8f8135e6362e298239a7d2d93455`. SHA256 por arquivo calculado dos bytes extraidos via `tar -xOf`, paths ordenados relativos ao pacote, sem duplicatas. Archive preservado; NAO publicado.
- Retomada: heavy independente verifica os corpos dos testes R2/R4, JSDoc de paths relativos, imports do limite compartilhado nos quatro modulos e fechamento commit/artefato. Nao repetir investigacao do incidente original como se a causa estivesse confirmada. Depois do gate P1, P2.T0 continua dependendo das autorizacoes individuais e do gate de comparacao PRE-publicacao aprovado no plano.

(Preencher por fase: pre-flight, comandos/CWD/exit code, QA heavy verdict, commits, bloqueios.)

- 2026-09-25: gate zero PASS com isolamento; adendo de mapeamento registrado; baseline coletada.
- 2026-09-25: `npm ci` em E-CAND (174 pacotes; aviso preexistente: postinstall do esbuild bloqueado por allowScripts — avaliar no gate de build). Baseline: `npm test` 99 arquivos/2043 passaram/2 skipped (14.7s); `npx vitest run wire-baseline` 16/16. Commit `7607c7d` `docs(plans): add unicode integrity fix plan, handover and execution log` (hooks verdes: suite completa + lint-staged).
- 2026-09-25: inicio do pre-flight P1 na biblioteca (`D:\git\claude-code-wire-compat`, HEAD `9c9dd6f`, branch `port/claude-code-2.1.280`).
- 2026-09-25: DECISAO P1.T1 (heavy, task ses_f252f7f84ffefCNr3f5n4barx2): D1 opcao (b) — body prose aceita todo escalar valido, so lone surrogate rejeitado; headers/identifiers/metadata estritos. D2 validador compartilhado em `src/unicode.ts` com presets PROSE/IDENTIFIER; `classifySurrogateAt` congelado; `headers.ts` fora do escopo. D3 `INVALID_UNICODE`=lone surrogate em todas as body lanes; `HEADER_INJECTION` inalterado; `INVALID_INPUT`=estrutural. D4 safeDetails: `violationReason` (closed set), `violationPath` (string, vocabulario fechado + `*` para chaves de usuario, <=16 segmentos, <=256 chars), `violationOffset`, `violationCodeUnit` (so ranges control/surrogate), `violationTextLength`, `violationInKey`; redaction.ts precisa de nova passagem de strings com revalidacao. D5 bump minor 0.7.0 sob "### Breaking" (0.6.0 ja publicado, registry latest=0.6.0); MEMORY.md com proveniencia (decisao local, sem evidencia upstream, gatilho incidente do plugin). Plano de commits: (1) test parity-matrix -> (2) refactor shared validator -> (3) fix! PROSE -> (4) feat safeDetails -> (5) chore release 0.7.0-rc.1. Probe remoto opcional documentado (count_tokens por code point) — NAO autorizado; nao e pre-condicao. Versao RC decidida: `0.7.0-rc.1`. Evidencias: (Q1) rejeicao de controles e politica defensiva LOCAL, sem proveniencia upstream (comentario `src/build-request.ts:247-249`; `CHANGELOG.md:463-478` sem citacao upstream; contraste com `CHANGELOG.md:352` que cita upstream para outra regra). (Q2) entry points publicos: `buildClaudeCodeRequest` (`validateInput` -> `inspectGraph` @792), `buildClaudeCodeCountTokensRequest` (`validateCountTokensInput` -> `inspectGraph` @899, cobre todas as chaves exceto crypto), `parseBuiltClaudeCodeRequest` (`inspectGraph` @1754); `src/request-body.ts:379-403` duplica inspector sem C1; `src/system-prompt.ts:43-61` rejeita 0x7F-0x9F; discrepancia C1 alcancavel (C1 passa em message/tool_result, rejeitado em system). (Q3) `ClaudeCodeWireError` (`src/contracts.ts:1252-1299`) aceita chaves string arbitrarias (exceto proto-pollution) com valores primitivos, frozen; stripping vem so do allowlist de `toSafeErrorDetails` (`src/redaction.ts:368+`) via `sanitizeError` (`src/build-request.ts:1008-1010`) nos tres entry points. `publish.yml` verificado: pack-once + publish do mesmo tarball; prerelease recebe `--tag beta` automatico; gate de identidade RC emendado no plano para comparacao de manifesto POS-publicacao/pre-adocao (sem mudanca de CI). Plano emendado em E-CAND (4.1 + P2.T0). Troubleshooting de dispatch: falhas `INVALID_UNICODE` recorrentes em dispatches que pediam excerpts brutos de docs; retry com restricao ASCII-only no output funcionou — consistente com hipotese (a) builder local 0.5.0 rejeitando conteudo do contexto, ainda UNVERIFIED. Proximo: decisao heavy P1.T1 (politica de texto de corpo).
- 2026-09-25: ROUTER TAKEOVER 1 (plano 3.2): commits P1 assumidos diretamente pelo orquestrador apos 2 falhas consecutivas de dispatch medium com `INVALID_UNICODE` (tasks ses_f252b3609ffe006CAPni4twsKy, ses_f252a80c9ffeA8I3Uw8FsopezT). Hipotese operacional: sessoes de subagente morrem ao ler fixtures com controles brutos (builder local 0.5.0 rejeita o contexto) — consistente com a bug em correcao. Mitigacao adotada: varredura de seguranca (contagens/line numbers apenas) antes de ler fixtures; `multiline-content.test.ts` verificado 0 caracteres perigosos. Retorno ao roteamento na proxima tarefa delegavel. npm ci na worktree da biblioteca: aviso preexistente — postinstall do workerd bloqueado por allowScripts (avaliar no gate test:pack).
- 2026-09-25: TOOLING (alegacao anterior RETRATADA): nao ha evidencia de que `apply_patch` nao escreva fora do workspace root. Aplicacoes atuais na biblioteca isolada foram confirmadas por diff e typecheck. Edicoes manuais continuam exclusivamente via `apply_patch`; nenhuma excecao autoriza Edit/Write.
- 2026-09-25: P1 COMMIT 1 `a46e5a2` `test: pin current body-prose control-character parity matrix` (takeover). Novo `test/validation/body-prose-parity-matrix.test.ts`: 115 células (12 code points x lanes userText/toolResult/toolUseInput/toolDescription/system x entries build/countTokens/parse) — 115/115 passam na primeira run, nenhuma surpresa vs evidencia. Divergencia C1 pinada explicitamente. Gates: lint/typecheck/build/format:check limpos; suite completa 117 arquivos/3452 testes verde. Count-tokens NAO aceita `system` (COUNT_TOKENS_INPUT_KEYS, build-request.ts:98-116).
- 2026-09-25: P1 COMMIT 2 `2afad0c` `refactor: share the body text validator through src/unicode.ts` (takeover). `inspectText`+`TextPolicy`+presets em unicode.ts; `classifySurrogateAt` intacto; 3 inspectors migrados reproduzindo comportamento exato (suite 3452 verde). Governanca `unicode-validation.test.ts` atualizada preservando a obrigacao (guarda NaN-safe unica; modulos consumidores proibidos de reimplementar limites surrogate).
- 2026-09-25: P1 COMMIT 3 `272bda8` `fix!: accept every well-formed UTF-16 string as body prose` (takeover). Switch PROSE nos 3 inspectors; `TEXT_POLICY_SYSTEM_LEGACY` removido; matriz atualizada (todas as células exceto lone surrogates -> accept); testes de 8 arquivos atualizados por camada (beta grammar INVALID_INPUT, header layer HEADER_INJECTION, metadata/identity estritos, model=body prose — verificado por probe offline); CHANGELOG `[0.7.0-rc.1]` undated + Breaking; MEMORY com proveniencia; source-trace L13 amendment; bump `0.7.0-rc.1` (package+lock+pin governanca) no mesmo commit (regra heading=manifest). Suite 117/3458 verde + lint/typecheck/build/format. Auditoria de identifier lanes: headers/metadata/identity tem validacao propria independente do graph walk (metadata.ts:183 confirma; caso Proxy de getter retardado segue rejeitando INVALID_UNICODE).
- 2026-09-25: P1 COMMIT 4 `58f652b` `feat: carry safe violation diagnostics in validation errors` (takeover). Novo `src/violation.ts` (vocabulario fechado de paths, `formatViolationPath` <=16 segmentos/<=256 chars com `/...`, `isValidViolation*`, `violationDetails`); `fail(code, details?)` nos 3 modulos; path tracking nos 3 graph walkers (build-request, request-body, system-prompt); redaction.ts com string pass (reason/path revalidados) + integer/range guards (offset/textLength/codeUnit) + violationInKey. Novo `test/validation/violation-details.test.ts` (e2e pos-sanitizeError, masking de chave de usuario com `*`, truncamento, hostile details descartados); mutantes atualizados de `safeDetails={}` para o novo shape. Incidente operacional: comando com NUL bruto rejeitado pelo Node — reforca regra de escapes.
- 2026-09-25: P1 COMMIT 5 `79d0a28` `test: cover the shared validator presets and violation helpers` (takeover). Cobertura: unicode.ts 100/92.59, violation.ts 94.87, global 99.04 stmts/98.72 branches/100 funcs/99.68+ lines (thresholds 98/97/100/99).
- 2026-09-25: P1 GATES COMPLETOS verdes: lint, typecheck, build, format:check, test (118 arquivos/3471), test:coverage, pack:check, fixtures:check (8 sealed ok), test:pack — digests congelados IDENTICOS em node/bun/workerd nos 3 perfis (2.1.280 = 9a531ed01ddd3440ce5b7f25c6caf5f045a9b4e78d885b5317508e21a22ada90), provando identidade de wire para inputs previamente aceitos.
- 2026-09-25: P1 CANDIDATO `0.7.0-rc.1`: tarball sha512 `133E7C0C40776C68B6E269F39AF79076BBA3196589A2110EA0880B4C7D181A064F58825AE48E2C63881AFDE173C63C724A42E21C76A9AB8E76154E82BF123D1A`; manifesto descompactado (140 arquivos, sha256 por arquivo) em `C:\Users\MARQUI~1\AppData\Local\Temp\opencode\rc1-manifest-sha256.txt`. Tarball descartado (reproduzivel do HEAD `79d0a28`). Branch `feat/unicode-body-prose`: 5 commits sobre `9c9dd6f`. Proximo: QA heavy de P1.
