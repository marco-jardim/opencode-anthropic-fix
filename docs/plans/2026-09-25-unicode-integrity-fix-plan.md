# Plano definitivo de integridade Unicode

Data: 2026-09-25. Estado historico na redacao: planejado, nao executado. Na execucao futura, registrar o estado atual em `D:\git\opencode-anthropic-fix\docs\plans\qa\unicode-integrity-execution.md`, sob o mapeamento de 1.1. Prompt operacional: `D:\git\opencode-anthropic-fix\docs\plans\2026-09-25-unicode-integrity-handover.md`.

Documento: `D:\git\opencode-anthropic-fix\docs\plans\2026-09-25-unicode-integrity-fix-plan.md`.

Repositorios: `D:\git\opencode-anthropic-fix` e `D:\git\claude-code-wire-compat`. Ambiente: Windows, win32, PowerShell 7 (pwsh). Todos os caminhos de arquivos neste plano e nos dispatches devem ser absolutos.

## 1. Objetivo e limites

Eliminar as causas confirmadas de `INVALID_UNICODE` produzidas por transformacoes locais; reconciliar a validacao de texto de corpo; impedir perda silenciosa no streaming UTF-8; preservar grafemas em cortes deliberados; entregar diagnosticos seguros e validar inferencia e display no host real. A garantia cobre o corpus e os contratos definidos abaixo, nao todos os renderizadores/fontes existentes.

Nao existem idiomas que dependam especificamente de UTF-16. Strings JavaScript usam unidades UTF-16; o transporte usa UTF-8; caracteres suplementares usam pares substitutos; caracteres percebidos pelo usuario podem conter varios pontos de codigo. Essas quatro unidades nao sao intercambiaveis com colunas de terminal ou tokens de modelo.

A sessao de redacao de 2026-09-25 autoriza somente documentacao. A futura execucao deve ser solicitada pelo usuario; quando enviado pelo usuario como instrucao, o prompt de handover solicita implementar o plano com commits LOCAIS frequentes. Ler esse arquivo como referencia nao inicia execucao nem concede permissoes. Publicacao, push, tags, deploy, requisicoes pagas/autenticadas, instalacao global e edicao do host exigem autorizacao especifica por acao/alvo (1.2); nao sao autorizados pela criacao destes documentos nem genericamente pelo handover.

Nao fazer refatoracao geral, migracao de schema de contas, mudanca de perfil Claude Code, introducao de TypeScript no plugin ou adicao de dependencia de producao sem necessidade demonstrada. A biblioteca permanece TypeScript, pura, I/O-free e runtime-neutral. Nao desabilitar permanentemente funcionalidades para aparentar resolver o problema.

### 1.1 Gate zero: isolamento do host vivo

Na execucao futura, antes de qualquer mutacao de runtime, install, build, formatter, coverage, pack ou commit nos dois repositorios, verificar somente leitura se o host carrega estes checkouts. A hipotese de junction para a worktree e UNVERIFIED; nao tratar como fato.

1. Resolver a entrada relevante do campo `plugin` da config do host por projecao segura (1.3), o realpath/target de junctions e o path real do modulo wire-compat carregado por esse plugin. Nao acessar tokens nem arquivos de contas.
2. Verificar ambos os repositorios e dependencias: worktree git isolada nao basta se `node_modules` ou o bundle continuam apontando para outro diretorio que sera editado/construido.
3. Se qualquer alvo de escrita for carregado pelo host vivo, preferir worktree isolada, mantendo junction/config e checkout vivos intocados. Alterar WIP usado pelo host requer consentimento especifico e informado; sem isolamento seguro ou esse consentimento, BLOCKED.
4. Os paths/CWDs canonicos neste plano sao defaults, nao autorizacao de escrita na instalacao viva. Antes de provisionar o isolamento, registrar na sessao a decisao, paths absolutos exatos e HEAD/base. Somente apos comprovar que o novo destino nao afeta os arquivos carregados, provisionar a worktree e persistir nela o adendo completo canonico -> isolado para paths, CWDs, owners, documentos e leases. Nao escrever primeiro o registro no checkout vivo para pedir isolamento depois. Nao mudar junction/config nem descartar mudancas locais existentes.
5. Esse adendo vincula TODOS os comandos e referencias seguintes, inclusive E-CAND e o registro de execucao. Resolver cada path absoluto antes de dispatch; nenhum placeholder ou substituicao silenciosa. Se nao for possivel confirmar isolamento e continuidade segura das sessoes em andamento, parar com BLOCKED.

Worktrees novas nao recebem documentos untracked automaticamente. Transferir somente plano/handover revisados por patch e conferir hashes, preservando os originais e quaisquer alteracoes externas. Registrar a procedencia; nao assumir que HEAD contem o planejamento atual.

### 1.2 Autorizacoes externas

Inventariar no boot as acoes previsiveis. Quando os alvos forem concretos, solicitar em um pedido objetivo as autorizacoes individuais: repo/branch para push, tag exata, workflow/trigger, `package@version`, dist-tag, probe live (endpoint/corpus/budget) e cada acao no host. "Publicar" nao implica autorizar push/tag. Alvos ainda nao decididos ficam PENDENTE; nao inventar versao nem exigir aprovacao prematura. P1 independente pode prosseguir sob seu pre-flight.

### 1.3 Projecao segura

Descobrir chaves pelo codigo `D:\git\opencode-anthropic-fix\lib\config.mjs`, nao abrindo a config do usuario. Usar parser/helper local confiavel que le localmente e emite somente booleans allowlisted, entrada relevante do plugin/path resolvido e erros enumerados. Nao ecoar JSON bruto, mensagens de excecao com conteudo, URLs com credenciais/query nem arquivos de contas. Para plugin remoto, emitir apenas identificacao sanitizada necessaria a resolucao. Se nenhuma ferramenta permitir projecao sem exposicao da config inteira, pedir projecao ao usuario ou autorizacao para helper local estreito; nao usar Read sobre a config bruta. Acesso externo ao workspace segue permissoes reais do ambiente.

## 2. Evidencias e incertezas

Snapshot historico de 2026-09-25; revalidar no pre-flight, especialmente U9/U10. A tabela nao garante versoes/configuracoes atuais.

| ID  | Evidencia confirmada                                                                                                                                                                                                                        | Consequencia e destino                                                                                    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| U1  | `D:\git\opencode-anthropic-fix\lib\mimicry\system-prompt.mjs`, `compactToolDescription`, corta com `slice(0, 200)`; reproducao com emoji na fronteira gerou surrogate isolado e erro do builder.                                            | Correcao definitiva em P2. Tambem ha corte de acento combinante sem UTF-16 malformado.                    |
| U2  | `D:\git\opencode-anthropic-fix\lib\rolling-summarizer.mjs`, `formatTemplate`, corta secao e envelope por unidades UTF-16; reproducao do resumo rejeitada pelo builder.                                                                      | Correcao definitiva em P2, incluindo budgets pequenos e progresso do algoritmo.                           |
| U3  | `D:\git\claude-code-wire-compat\src\build-request.ts`, `D:\git\claude-code-wire-compat\src\request-body.ts` e `D:\git\claude-code-wire-compat\src\system-prompt.ts` rejeitam controles validos como politica; C1 e codigo de erro divergem. | Contrato unificado de texto em P1; headers/identificadores nao devem ser relaxados.                       |
| U4  | U+0085 passa em mensagem e falha em system; ESC, NUL, FF e DEL sao rejeitados localmente. A rejeicao ocorre antes de fetch.                                                                                                                 | Nao atribuir a restricao a Anthropic sem evidencia. A politica de corpo e uma decisao explicita em P1.    |
| U5  | `D:\git\opencode-anthropic-fix\lib\mimicry\response-stream.mjs` usa decoder incremental, mas nao faz flush no EOF; F0 9F finais desaparecem.                                                                                                | Decoder estrito, flush e erro de stream definido em P3. UTF-8 invalido nao deve virar sucesso silencioso. |
| U6  | Normalizacao CRLF por chunk perde enquadramento quando CR/LF se separam; duas mensagens usage byte a byte preservam texto, mas nao disparam `onUsage`.                                                                                      | Parser incremental de linhas/eventos em P3, sem normalizar conteudo JSON.                                 |
| U7  | Captura SSE denominada bytes usa comprimento UTF-16; cortes de diagnostico podem dividir texto.                                                                                                                                             | Budgets reais e saida local segura em P3. Nao confundir captura com payload enviado.                      |
| U8  | `D:\git\claude-code-wire-compat\src\redaction.ts`, `toSafeErrorDetails`, filtra campos; detalhes adicionados apenas ao erro seriam removidos.                                                                                               | Diagnostico seguro de ponta a ponta em P1 e consumo no plugin em P2.                                      |
| U9  | `D:\git\opencode-anthropic-fix\package-lock.json` fixa `0.5.0`; `D:\git\opencode-anthropic-fix\node_modules\@tormentalabs\claude-code-wire-compat\package.json` confirma `0.5.0`. Registro npm consultado: `latest=0.6.0`.                  | Instalacao/lockfile desatualizados, nao ausencia de publicacao. Corrigir adocao por registry em P4.       |
| U10 | `D:\git\claude-code-wire-compat\package.json` esta em `0.6.0`, e as validacoes problematicas continuam no fonte.                                                                                                                            | Atualizar somente para `0.6.0` nao resolve U1-U8. A release final precisa conter as correcoes.            |

Controles sao valores Unicode validos; surrogates isolados nao sao valores escalares Unicode. `JSON.stringify` escapa surrogates isolados; `TextEncoder` substitui esses surrogates quando recebe a string bruta. Nao descrever esses comportamentos como iguais.

O corpus multilingue preservou texto nos 160 cortes de duas partes testados no SSE valido, inclusive em chunks de um byte. Isso nao testa a TUI. As quatro suites direcionadas de transformacao/stream/resumo passaram 41 testes existentes; a suite multiline da biblioteca passou 34. Esses resultados sao baseline historico, nao prova da correcao futura.

A configuracao efetiva do usuario, o campo exato que abortou a sessao e o renderer instalado ainda nao foram identificados. Compactacao de descricoes e resumo Haiku sao opt-in por default. Nao afirmar que U1 ou U2 foi o gatilho original sem evidencia da configuracao e diagnostico seguro.

A causa de `INVALID_UNICODE` nos dispatches da sessao anterior e UNVERIFIED: pode ser o builder local antes do fetch ou rejeicao do provedor/transporte. Distinguir por stack segura e trace/status/request-id correlacionados ao mesmo request; ausencia de HTTP sozinha nao prova falha local (pode ser timeout), nem request-id alheio prova rejeicao remota. Nenhuma hipotese identifica por si so o campo original ou prova ausencia de ferramenta. Exemplos enviados ao LLM devem usar escapes ASCII, nunca controles brutos/surrogates isolados.

## 3. Diretivas de execucao

### 3.1 Continuidade e bloqueios

Executar o plano inteiro, wave por wave, sem pedir confirmacao entre tarefas, fases ou waves. Cada fase entrega uma correcao completa de seu subobjetivo, com integracao, testes e QA; nao criar fases intermediarias de logging, scaffolding ou testes que deixem seu subobjetivo quebrado.

Ciclo obrigatorio: pre-flight -> implementar -> ampliar testes e executar gates -> senior QA heavy adversarial -> corrigir todos os achados -> repetir testes e QA -> commit verde -> proxima fase. Commits menores durante a fase sao encorajados; nao substituem o gate final.

Interromper somente por ambiguidade que precise de decisao humana, risco critico ou bloqueio real. Exemplos: politica remota sem evidencia suficiente, segredo exposto, drift de wire sem justificativa, impossibilidade de executar gate obrigatorio, host indisponivel, publicacao nao autorizada ou indisponibilidade persistente de QA heavy. Reportar evidencia, impacto, tentativas e pergunta objetiva. Continuar apenas trabalhos independentes e seguros; nunca declarar a fase dependente concluida.

Apos tres tentativas consecutivas sem resolver a mesma falha, parar a tentativa, preservar evidencias e consultar RCA heavy. Recuperar apenas alteracoes proprias com patch revisado, sem apagar trabalho alheio, reset destrutivo ou ocultar teste falhando. Persistindo o bloqueio, pedir esclarecimento.

Iterar continuamente ate concluir ou encontrar bloqueio real/risco critico; nao encerrar por mera friccao de orquestracao. Busca vazia valida, aviso de CAP/budget ou resposta legitimamente breve nao sao falhas. Respeitar limites operacionais e reunir contexto suficiente, em vez de repetir chamadas sem progresso.

Hand-backs estruturados com lacuna concreta (`SCOPE GROWTH`, `NEED CONTEXT`, `ESCALATE`) nao sao recusa: coletar contexto limitado via fast e redespachar com escopo adequado. Heavy pedindo contexto nao esta indisponivel. Enquanto P2 estiver BLOCKED, trabalho independente no plugin limita-se a preparacao read-only, evidencia de protocolo e probes offline isolados; sem commits de runtime/testes ou P3 contornando P2.T0. P1 independente continua sob seu proprio pre-flight.

### 3.2 Model-router

| Anotacao        | Responsabilidade                                                                                                 |
| --------------- | ---------------------------------------------------------------------------------------------------------------- |
| `[tier:fast]`   | Inventario, leitura, busca, coleta de provas, execucao de comandos e coleta de logs. Nao aprova QA.              |
| `[tier:medium]` | Implementacao, testes, integracao, documentacao e remediacao dos achados.                                        |
| `[tier:heavy]`  | Senior QA engineer adversarial em TODAS as fases e no gate global; decisoes de contrato/security e RCA complexo. |

QA e SEMPRE uma tarefa heavy, sem downgrade para fast/medium ou autoaprovacao do implementador. Delegar o QA a um agente heavy independente. O autor nao e o grader. Um resultado rejeitado, sem verdict parseavel ou com erro de transporte nao e revisao aprovada.

Para falhas operacionais de QA: uma chamada heavy inicial e no maximo uma recuperacao heavy-class no provedor alternativo permitido pelo router; persistindo indisponibilidade, BLOCKED. Esse limite nao limita ciclos de remediacao de achados nem coleta legitima de contexto. Reviewer independente nao escreveu nem assumiu a implementacao; pode reavaliar as correcoes na mesma sessao de review.

Antes de chamar heavy, fast prepara contexto estavel: diff completo, fontes relevantes, invariantes, testes/saida, baseline e perguntas adversariais. Heavy raciocina sobre esse material, pede contexto adicional se necessario e nao e usado como executor de suite. Se o ambiente nao permite delegacao heavy independente, registrar bloqueio de verificacao e nao marcar PASS.

Todo dispatch contem: TASK, EXPECTED OUTCOME, TOOLS (informativo), MUST DO, MUST NOT DO, CONTEXT e ENVIRONMENT. ENVIRONMENT informa diretorio absoluto exato, win32, pwsh e worktree atribuida. Incluir `owns`, `reads`, dependencias, tier, CAP apropriado e criterios verificaveis; nunca enviar segredos ou caracteres de controle brutos ao LLM. Para casos malformados usar escapes ASCII e offsets numericos.

Usar acceptance blocks com checks realmente disponiveis, por exemplo `testsPass` e `buildPasses` nos repositorios que possuem esses scripts. Para QA, exigir verdict e evidencia, nao aceitar um build verde como substituto de review.

Recuperacao do model-router: manter o roteamento ordinario. Se falha concreta (transporte, recusa falsa ou saida prolixa/truncada que nao entrega o solicitado) bloquear leitura/implementacao, registrar evidencia e fazer uma tentativa limitada de recuperacao no tier/provedor alternativo previsto pelo router autoritativo. Se tambem falhar, o orquestrador assume temporariamente somente a operacao bloqueada, com verificacao independente posterior, e retorna ao roteamento na proxima tarefa. Nao insistir indefinidamente em agente incapaz de entregar nem usar takeover para ignorar um resultado valido ou restricao de seguranca. A regra de tres falhas tecnicas iguais e o RCA continuam valendo.

Excecao que NAO existe: assumir ou rebaixar QA. QA e sempre delegado a heavy independente, adversarial, apos cada fase e globalmente, mesmo quando o orquestrador implementou diretamente. Indisponibilidade persistente de heavy apos tentativas limitadas bloqueia o gate; continuar apenas trabalho independente seguro, sem autoaprovacao, PASS falso ou fechamento da fase.

Antes de retry, substituicao ou takeover que possa escrever: confirmar que o writer anterior terminou; revogar um lease nao interrompe um processo. Inspecionar diff parcial, HEAD e hashes contra o snapshot; registrar a transferencia de leases. Se o estado do writer for desconhecido, bloquear esses paths, sem escritor concorrente.

### 3.3 Paralelismo seguro

O orquestrador mantem um registro unico de leases com agente, worktree, paths canonicos absolutos, modo READ/WRITE, revisao-base e status. Normalizar case, junctions e aliases no Windows: dois nomes para o mesmo arquivo sao o mesmo recurso.

1. Um arquivo tem no maximo um writer; durante WRITE, nenhum OUTRO agente pode le-lo. Varios READ sao permitidos apenas sem WRITE. Novos arquivos e seus nomes tambem precisam de lease.
2. O dono pode ler seus proprios arquivos para editar. Fontes compartilhadas devem ser lidas antes do fan-out e congeladas em snapshot imutavel ou receber READ lease. Nao abrir a versao viva enquanto outro agente escreve.
3. `D:\git\opencode-anthropic-fix\index.mjs` e um lock integral, nao divisivel por funcoes. O mesmo vale para qualquer teste ou documento compartilhado, inclusive este plano e o registro de QA.
4. `D:\git\claude-code-wire-compat\AGENTS.md` proibe dois writing tasks na mesma worktree. Respeitar essa restricao mais forte: um writer total por worktree dessa biblioteca. Paralelizar entre repositorios ou snapshots/worktrees isoladas, nunca burlando a regra com arquivos distintos.
5. No plugin, permitir writers em conjuntos disjuntos apenas quando a intersecao de WRITE com WRITE/READ de outros agentes for vazia. Tests, fixtures, manifests e docs acompanham seu dono; nao dar os mesmos testes a um agente separado.
6. Test runner, build, coverage, formatter, npm install/update/ci, git staging e hooks tambem leem/escrevem arquivos. Congelar a worktree antes de gates de fase, QA e commit; o executor de comandos detem leases de todos os recursos implicados. Nao rodar suite enquanto outro agente edita suas dependencias.
7. Commits, installs, builds e formatters de escopo global sao serializados por worktree. Nao rodar comandos que sobrescrevam o mesmo output em paralelo. Cross-repo gates podem rodar juntos apos freeze.
8. Novas worktrees so quando necessarias e verificadas antes de criar; cada uma recebe path absoluto e commit-base. Integracao por um unico agente, sem duas escritas na worktree destino. Testar novamente a composicao, nao apenas branches isoladas.
9. Ao terminar, o writer entrega patch/commit e relinquishes leases. Fast verifica estado/hash; so entao libera novos readers. Se um hash mudar desde o snapshot, invalidar a evidencia e repetir o gate afetado.

Paralelismo recomendado: durante P1, P2 limita-se a leituras de preparacao sobre snapshots imutaveis, sem escrita/commit de runtime/testes no plugin. A administracao de plano/handover/registro segue 3.4, sob isolamento e leases. Escritas de runtime/testes P2.T1-T6 esperam P1 QA PASS e o gate P2.T0. Dentro de P2, compactacao e resumo podem correr em paralelo depois de congelado o helper compartilhado. P3 tem um dono de stream e um dono de diagnosticos/entrada, com integracao serializada. P4 nao disputa arquivos com nenhuma fase anterior.

### 3.4 Commit often

Edicoes manuais usam `apply_patch`, conforme instrucao de maior prioridade. Delegado sem essa ferramenta trabalha patch-only sob READ, sem WRITE: devolve proposta, encerra leituras e transfere ao orquestrador, unico writer da aplicacao/verificacao. Isso nao cria excecao para escrita concorrente nem bypass geral do router.

Durante a execucao autorizada, fazer commit por unidade coerente e verde de correcao mais testes; nao acumular uma wave inteira nem commitar testes vermelhos isolados. Nao impor uma fase adicional para cada commit.

Antes de cada commit, inspecionar `git status`, `git diff` e `git log --oneline -10` no repositorio correto. Stage somente paths absolutos pretendidos; nunca `git add -A`. Usar Conventional Commits, referencia W/P/T e `git commit -s` conforme a biblioteca. Nao alterar identidade Git; identidade ausente e bloqueio. Nao amend, force push, skip hooks ou reset destrutivo.

Congelar todos os writers para que hooks nao leiam arquivos em edicao. Revisar alteracoes de formatter/hooks e reexecutar gates afetados antes de aceitar o commit. Nao commitar credenciais, capturas reais, outputs de build, tarballs ou modificacoes de terceiros. Um commit nao implica permissao para publish/push/tag.

Na execucao futura, apos baseline e QA heavy documental, incluir intencionalmente plano/handover em primeiro commit `docs:` dedicado, com paths mapeados pelo gate zero, sem mudancas externas nao relacionadas. Registrar snapshots sanitizados do execution log nas fronteiras de fase sob freeze. Esta diretiva nao solicita commits na sessao de redacao.

## 4. Pre-flight, QA e evidencia comuns

### 4.1 Pre-flight obrigatorio antes de CADA fase

`[tier:fast]` coleta; `[tier:heavy]` decide apenas riscos/ambiguidades encontrados; `[tier:medium]` corrige preparacao tecnica necessaria.

1. Confirmar gate zero (1.1) antes de mutacoes; resolver mapeamento de paths se necessario. Confirmar diretorios, branches, HEADs, status staged/unstaged e alteracoes externas nos dois repositorios. Registrar baseline sem desfazer trabalho do usuario.
2. Ler as instrucoes atuais em `D:\git\opencode-anthropic-fix\AGENTS.md`, `D:\git\opencode-anthropic-fix\CLAUDE.md`, `D:\git\opencode-anthropic-fix\CONTRIBUTING.md`, `D:\git\claude-code-wire-compat\AGENTS.md` e `D:\git\claude-code-wire-compat\MEMORY.md`; inspecionar configs e 2-3 arquivos analogos do escopo.
3. Registrar Node/npm/Bun/workerd quando aplicavel, disponibilidade de `Intl.Segmenter`, ICU e scripts atuais. A dependencia instalada exige Node >=20; nao presumir suporte Node 18 por descricao antiga do plugin. Validar no minimo Node 20/22/24 para a biblioteca conforme sua CI.
4. Confirmar versoes do manifesto, lockfile, pacote realmente resolvido e bundle realmente carregado. Registrar perfil efetivo sem muda-lo. Revalidar registry somente quando necessario; nao perseguir `latest` durante a execucao.
5. Coletar configuracao efetiva das flags somente via projecao segura (1.3) e identificar o host usado. Nunca enviar arquivos de contas/configuracao inteira aos agentes. Nao ligar flags na instalacao real para reproduzir.
6. Executar baseline das suites afetadas e comandos requeridos pela fase em arvore estavel. Separar falhas preexistentes das reproducoes novas esperadas; nao deletar/skippar testes ou reduzir thresholds.
7. Congelar contratos de input/output, mapa de owners/reads e lista de testes. Verificar releases/autorizacoes/host exigidos pela fase. Nao iniciar escrita se esses gates forem bloqueantes.
8. Registrar PASS/BLOCKED, comandos e exit codes em `D:\git\opencode-anthropic-fix\docs\plans\qa\unicode-integrity-execution.md` (novo arquivo de execucao, dono unico: orquestrador).

O gate de adocao de dependencia em `D:\git\opencode-anthropic-fix\CONTRIBUTING.md` e obrigatorio: atualizar via registry, revisar lockfile, rodar suite completa e `npx vitest run wire-baseline` ANTES de editar codigo do plugin sobre essa nova baseline. Aplicar o gate em P2.T0 para o candidato rc e novamente na adocao estavel em P4.T7. A baseline instalada anterior e registrada em E-BASE; nao e necessario um bump intermediario para `0.6.0` se o candidato corrigido ja estiver disponivel. Nao perseguir `latest` nem atualizar silenciosamente para versao diferente da aprovada.

Mecanismo de candidato: prerelease imutavel `X.Y.Z-rc.N` no npm registry, publicado fora de `latest`, com autorizacao explicita futura. O classificador em `D:\git\opencode-anthropic-fix\test\conformance\package-dependency-policy.test.mjs` aceita versoes exatas com sufixo prerelease; revalidar essa regra no pre-flight. O publisher pode usar canal `beta`, mas o specifier do plugin deve ser a versao exata, nunca o dist-tag `beta`. O pin temporario tem justificativa de integracao e provenance em `D:\git\opencode-anthropic-fix\docs\shared-package-provenance.md`, com versao/resolved/integrity. Nao usar a excecao historica de tarball GitHub para contornar o gate de registry, nem relaxar o classificador. Sem autorizacao de publicacao/push/tag, P2 fica BLOCKED, sem PASS parcial. Tarball local serve aos gates de pack da biblioteca, nao ao lockfile do plugin. Nada e publicado por esta edicao do plano.

Identidade do RC, aplicavel a P1/P2.T0: registrar sha512 do tarball local e manifesto descompactado com hash por arquivo. O workflow deve verificar dist-tag explicita nao-`latest` e comparar, ANTES de publicar, o manifesto rebuilt com o candidato aprovado, sem mudanca de fonte/runtime. Depois, comparar o manifesto do registry e registrar ambos os hashes de container e a integrity publicada. Containers podem diferir sem diferenca de conteudo; isso nao autoriza drift de arquivos. Verificar capacidade do workflow read-only ja no pre-flight P1. Ausencia desses gates bloqueia publicacao ou exige mudanca minima de CI previamente autorizada, com owner/lease e QA heavy antes de publicar. Nao afirmar que o workflow atual tem ou nao tem esses gates sem ler.

Se aprovado esse ajuste de CI, o release owner assume `D:\git\claude-code-wire-compat\.github\workflows\publish.yml` (sob mapeamento 1.1); enumerar e congelar o escopo antes de escrever. A autorizacao de publish nao autoriza automaticamente alterar CI, push ou tags.

### 4.2 Senior QA heavy apos CADA fase

`[tier:fast]` prepara evidence pack da revisao congelada. `[tier:heavy]` independente recebe o papel: "Senior QA engineer adversarial: tente invalidar os criterios de aceitacao com contraexemplos, nao apenas confirmar testes existentes".

Review obrigatorio: integridade de unidades Unicode/grafemas, perda/duplicacao de texto, limites e complexidade, lifecycle/cancelamento, preservacao do wire, seguranca/redaction, comportamento de falhas, contrato publico, cobertura negativa e ausencia de testes tautologicos. Exigir contraexemplos alem dos escritos pelo produtor.

Cada achado registra ID, severidade, path absoluto/linha, reproducao, esperado/obtido e criterio violado. Verdict: `PASS` ou `CHANGES_REQUIRED`. `[tier:medium]` corrige TODOS os achados validos, inclusive de baixa severidade; adiciona regressao quando aplicavel; `[tier:fast]` repete os gates; `[tier:heavy]` reavalia a revisao final. Um falso positivo so fecha com evidencia e concordancia do reviewer, nunca por adiamento silencioso.

Nao encerrar fase com achado valido aberto. Se o QA descobrir falha fora da fronteira da fase, ampliar a correcao e invalidar gates dependentes; se exigir decisao humana ou inviabilizar o contrato, bloquear. Nao mover achados para um backlog para obter PASS.

### 4.3 Formato de evidencia

No registro unico de execucao, cada fase deve ter: pre-flight, HEAD/base, hash da revisao revisada, owners, tarefas/subtarefas, comandos/CWD/exit code, contagens reais, cobertura, seeds de geracao, evidencias de mutantes/casos negativos, commits, rodadas de QA e verdict final. Fast fornece evidencias ao orquestrador sem escrever concorrentemente no registro.

Nao armazenar payloads reais nem snippets sensiveis. Corpus sintetico, escapes ASCII e codigos numericos bastam. Uma ferramenta indisponivel e UNVERIFIED/BLOCKED, nunca PASS.

## 5. Contratos invariantes

| Contrato                   | Regra definitiva                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Texto valido               | Preservar pontos de codigo originais, sem NFC/NFD global, transliteracao, remocao generica de caracteres ou dupla serializacao.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Surrogate isolado original | Rejeitar com erro local seguro e acionavel, antes de enviar. Nao usar `toWellFormed` para esconder corrupcao nem fingir que dados invalidos podem ser enviados sem perdas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Transformacao opcional     | Para entrada valida, produzir saida valida; se a transformacao falhar, restaurar somente o original valido daquela transformacao, com diagnostico seguro e sem desativar sanitizacao obrigatoria. Revalidar limites de tamanho/contexto: fallback nunca burla um hard cap.                                                                                                                                                                                                                                                                                                                                                                                       |
| Corte intencional          | Somente em fronteira de grafema; unidade de budget explicita. Garantir progresso e respeitar teto, incluindo sufixo/envelope. Nao prometer preservar texto que foi intencionalmente resumido, mas nunca cortar um grafema ao meio.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Conteudo de inferencia     | Nao tratar ANSI/OSC como comandos nem apagar controles genericamente no caminho para o modelo. Politica do corpo distinta da politica de exibicao.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| UTF-8 de resposta          | Decodificacao incremental estrita, flush final e erro explicito se invalido/incompleto. U+FFFD originalmente valido deve ser preservado, nao confundido com substituicao do decoder.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Unicode dentro de JSON     | UTF-8 valido nao prova que strings obtidas por JSON.parse estejam bem formadas: escapes podem representar surrogates isolados. Definir validacao no nivel semantico correto de cada bloco, sem confundir fragmentos de JSON de tool input com documentos completos.                                                                                                                                                                                                                                                                                                                                                                                              |
| SSE                        | Resultados independentes da particao dos bytes. Delimitadores nao sao conteudo JSON. Campos e tool names so mudam pelas transformacoes documentadas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Headers/metadata           | Manter defesa contra CRLF/header injection e regras de identificadores, mesmo com relaxamento justificado do texto de corpo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Wire/fingerprint           | Nao alterar amostragem UTF-16 dos indices 4/7/20 em `D:\git\claude-code-wire-compat\src\fingerprint.ts`; e comportamento intencional do upstream. Preservar perfis, betas, headers e corretude de hashes. Identidade de bytes vale para entradas/caminhos NAO afetados pela correcao; cortes que antes quebravam grafema (ou surrogate) nao sao baseline protegida. Toda diferenca intencional exige registro auditado (entrada, bytes antigos, bytes novos, justificativa), escopo de versao/perfil/flag e oraculo independente com regressao. Nao existe permissao generica de reseal; vetores golden genuinos do upstream na biblioteca permanecem intocados. |
| Erros/contas               | Erro deterministico local nao causa rotacao, cooldown de conta ou retry de rede. Stream parcialmente entregue nao pode ser reexecutado silenciosamente, duplicando resposta/tool calls. Usage faturavel ja recebido e contabilizado separadamente do sucesso: nunca apagado em erro, nunca contado duas vezes, e erro de decoder local nao penaliza conta.                                                                                                                                                                                                                                                                                                       |
| Terminal de stream         | EOF, validade UTF-8 e completude de frame SSE sao distintos do evento terminal do protocolo. O plugin rastreia o estado terminal do stream Anthropic transformado; EOF sem terminal valido e erro reportado ao host, sem sucesso e sem replay.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Display                    | Budgets de bytes, grafemas e colunas visuais sao distintos. O plugin nao controla a fonte/TUI do host; a garantia visual exige P4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

## 6. Waves e dependencias

| Wave | Fase | Subobjetivo completo                                                                             | Dependencias                                                                                |
| ---- | ---- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| W1   | P1   | Contrato Unicode da biblioteca e diagnostico publico seguro, ponta a ponta.                      | Pre-flight, fonte de verdade da politica e baseline congelada.                              |
| W1   | P2   | Transformacoes do plugin que nunca geram texto malformado ou grafema parcial; integracao com P1. | P1 QA PASS; publicacao autorizada do candidato e gate P2.T0 antes de editar runtime/testes. |
| W2   | P3   | Streaming, EOF, framing, captura e exibicao local seguros, como um fluxo completo.               | P1/P2 verdes; nenhuma escrita concorrente da wave anterior.                                 |
| W3   | P4   | Adocao real da release corrigida, testes do pacote/bundle e inferencia/display no host.          | P1-P3 verdes; release e ambiente de host acessiveis/autorizados.                            |

Nao criar wave de "apenas observabilidade" nem tratar update `0.5.0 -> 0.6.0` como correcao central. Gates finais sao parte da fase que entrega o comportamento.

## 7. W1 / P1 - Biblioteca: texto e erros

### Pre-flight P1

Executar integralmente 4.1. Ler validadores e call graph publicos, schemas, governanca Unicode, redaction e memoria; confirmar entradas de messages e count-tokens e campos efetivamente textuais. Reproduzir rejeicoes com strings construidas por escapes, sem API real.

`[tier:fast]` coleta evidencia first-party sobre serializacao/aceitacao de controles no corpo do cliente/API; `[tier:heavy]` fixa contrato. Default proposto: texto JSON bem formado preserva valores escalares validos, inclusive controles escapados pelo serializador; surrogates isolados continuam proibidos. Nao aplicar esse default a headers, nomes de ferramentas ou metadata. Se a evidencia nao resolver se o endpoint suporta os casos ou houver contradicao, parar essa decisao e pedir ao humano autorizacao para probe sintetico ou escolha explicita. Nao inventar restricao remota nem chamar a suite local de prova da API.

### Tasks e subtasks

| Task  | Tier                           | Subtasks e entrega                                                                                                                                                                                                                                                                                                                        |
| ----- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1.T1 | `[tier:heavy]`                 | Fechar matriz de campos e codigos de erro: texto, objeto/chave, schema de ferramenta, identificador e header. Definir compatibilidade dos exports existentes e taxonomia; nao adicionar codigos publicos sem verificar consumidores.                                                                                                      |
| P1.T2 | `[tier:medium]`                | Consolidar apenas a validacao realmente compartilhada; aplicar o contrato em graph/body/system e caminhos publicos. Preservar limites de profundidade/tamanho/ciclos, prototipos, ordem de validacao segura e pureza.                                                                                                                     |
| P1.T3 | `[tier:medium]`                | Propagar localizacao segura em erro -> sanitizacao -> public API. Atualizar allowlist/validacao de `safeDetails`; incluir unidade/offset e motivo enumerado, com limites. Caminho somente estrutural validado + indices, sem chaves arbitrarias do usuario. Para chaves/conteudo de tool input, usar localizacao opaca sem nome sensivel. |
| P1.T4 | `[tier:medium]`                | Adicionar testes publicos, propriedades/seeds e documentacao da politica. Preparar versao `X.Y.Z-rc.N`, metadados e guards de release/export; produzir tarball com digest para gates de consumidores empacotados. Validar esse candidato pelos gates completos; nao publicar nesta fase.                                                  |
| P1.T5 | `[tier:fast]` + `[tier:heavy]` | Executar gates abaixo; QA adversarial independente; remediar via medium e repetir ate PASS.                                                                                                                                                                                                                                               |

Owner da biblioteca e unico por worktree. Escopo principal: `D:\git\claude-code-wire-compat\src\unicode.ts`, `D:\git\claude-code-wire-compat\src\build-request.ts`, `D:\git\claude-code-wire-compat\src\request-body.ts`, `D:\git\claude-code-wire-compat\src\system-prompt.ts`, `D:\git\claude-code-wire-compat\src\redaction.ts` e `D:\git\claude-code-wire-compat\src\contracts.ts`. So editar o que a solucao exigir; nao refatorar headers/fingerprint por conveniencia.

Tests/docs do mesmo owner: `D:\git\claude-code-wire-compat\test\validation\unicode.test.ts`, `D:\git\claude-code-wire-compat\test\validation\multiline-content.test.ts`, `D:\git\claude-code-wire-compat\test\validation\redaction.test.ts`, `D:\git\claude-code-wire-compat\test\redaction.test.ts`, `D:\git\claude-code-wire-compat\test\governance\unicode-validation.test.ts`, `D:\git\claude-code-wire-compat\README.md` e `D:\git\claude-code-wire-compat\MEMORY.md`. Governanca nao deve ser enfraquecida: qualquer ajuste deve preservar a obrigacao que ela fiscaliza.

### Novos testes e edge cases no fechamento P1

1. Corpus em system, user/assistant text, tool description, tool result e tool input/schema: ASCII, portugues, NFC/NFD distintos, arabe/bidi, CJK, Devanagari, U+20000, emoji, modificador de pele, ZWJ, bandeira e variation selector. Assertar igualdade do texto apos parse JSON, nao apenas ausencia de throw.
2. TAB/LF/CR, NUL, ESC, VT, FF, DEL e cada C1 contra a matriz aprovada, em todas as APIs exportadas relevantes; mensagens e count-tokens. Distinguir controles reais de texto literal com backslash.
3. High/low surrogate isolado no inicio/meio/fim, par invertido, high-high, high seguido de ASCII e chave malformada. Pares validos nos mesmos offsets devem passar. Exigir erro antes de fetch no consumidor.
4. `safeDetails` valido sobrevive a `sanitizeError` e `toSafeErrorDetails`; campos desconhecidos, prototipos perigosos, valores nao finitos, paths forjados/longos e dados secretos sao removidos/rejeitados. Capturar erro inteiro e confirmar que nao contem tokens, prompts ou nomes arbitrarios.
5. CRLF injection em headers continua rejeitada. Regressao de metadata/identificadores, max size/depth, ciclos e objeto com prototipo inesperado. Contagem de bytes inclui UTF-8 multibyte, nao `.length`.
6. Usar `fast-check` ja existente na biblioteca para strings escalares validas e mutacoes malformadas, com seeds reproduziveis. Novos testes de validacao devem exercitar `D:\git\claude-code-wire-compat\src\index.ts` conforme governanca.

Gates em CWD `D:\git\claude-code-wire-compat`: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, `npm run format:check`, `npm run test:coverage`, `npm run pack:check`, `npm run test:pack`, `npm run fixtures:check`. Executar sequencialmente quando houver outputs compartilhados. Manter digests congelados e suites diferenciais, sem reseal oportunista.

### Aceitacao e DoD P1

Aceitacao: matriz de texto consistente nos caminhos publicos, nenhum falso `INVALID_UNICODE` para os casos aprovados, nenhum surrogate isolado aceito, headers estritos, dados validos preservados e diagnostico seguro nao esvaziado. Testes provam o comportamento do artefato empacotado, nao apenas imports privados.

Metadados do candidato pertencem ao mesmo owner P1: `D:\git\claude-code-wire-compat\package.json`, `D:\git\claude-code-wire-compat\package-lock.json`, `D:\git\claude-code-wire-compat\CHANGELOG.md` e guards de release identificados por path absoluto no pre-flight. Transferir esses leases ao release owner somente depois do freeze de P1.

DoD: pre-flight PASS; P1.T1-T5 completos; todos os gates verdes; cobertura nao rebaixada; QA heavy PASS sem achados abertos; docs e commits coerentes; candidato `X.Y.Z-rc.N`, tarball/commit identificados para P2 com sha512 local e manifesto descompactado/hash por arquivo registrados (4.1). Thresholds historicos de `D:\git\claude-code-wire-compat\vitest.config.ts`: 98% statements, 99% lines, 100% functions e 97% branches; revalidar e nao reduzir. Publicacao nao ocorre em P1.

## 8. W1 / P2 - Plugin: transformacoes e protecao local

### Pre-flight P2

Executar 4.1 e registrar o path absoluto de E-BASE antes do uso. Confirmar autorizacao de publicacao do candidato e P1 QA PASS. P2.T0 deve passar antes de qualquer edit de runtime/testes do plugin; verificar que U1/U2 ainda reproduzem com o candidato adotado. Congelar contrato/helper antes do fan-out; ler sem concorrencia o hook de resumo em `D:\git\opencode-anthropic-fix\index.mjs` e as flags em `D:\git\opencode-anthropic-fix\lib\config.mjs`. Confirmar `Intl.Segmenter` nos runtimes suportados, incluindo candidatos a clusters muito longos.

### Tasks e subtasks

| Task  | Tier                            | Subtasks e entrega                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P2.T0 | `[tier:medium]` + `[tier:fast]` | Apos P1 QA PASS e autorizacoes individuais: comparar manifesto rebuilt ao candidato aprovado ANTES de publicar e conferir tag nao-latest (4.1). Publicar pelo fluxo OIDC, comparar manifesto do registry, registrar hashes de container e integrity. Adotar em E-CAND, atualizar provenance e executar suite completa existente mais `npx vitest run wire-baseline` antes de editar runtime/testes. Divergencia de arquivos bloqueia; commit do bump somente verde. |
| P2.T1 | `[tier:medium]`                 | Definir primitiva pequena reutilizavel de prefixo por grafema, se realmente compartilhada. Candidato novo: `D:\git\opencode-anthropic-fix\lib\unicode-text.mjs`, com `D:\git\opencode-anthropic-fix\lib\unicode-text.test.mjs`. Budget UTF-16 legado e budget UTF-8 devem ter nomes/contratos distintos. Congelar helper antes dos consumidores.                                                                                                                    |
| P2.T2 | `[tier:medium]`                 | Corrigir compactacao de descricoes em `D:\git\opencode-anthropic-fix\lib\mimicry\system-prompt.mjs` e integracao em `D:\git\opencode-anthropic-fix\lib\mimicry\request-body.mjs`. Preservar grafema inteiro no limite; contar sufixo conforme contrato documentado, sem transformar historico original.                                                                                                                                                             |
| P2.T3 | `[tier:medium]`                 | Corrigir cortes de secoes/envelope em `D:\git\opencode-anthropic-fix\lib\rolling-summarizer.mjs`. Para budget menor que envelope minimo, usar o fallback existente do host com motivo seguro; nao emitir envelope quebrado. Reducao por cluster precisa progredir, mesmo quando um grafema excede o budget.                                                                                                                                                         |
| P2.T4 | `[tier:medium]`                 | Integrar fallback local e diagnostico em `D:\git\opencode-anthropic-fix\index.mjs` e `D:\git\opencode-anthropic-fix\lib\mimicry\wire-compat.mjs`. Preservar origem valida por transformacao; nao restaurar payload inteiro burlando sanitizacao, cache policy ou prefixes. Invalidade original falha com detalhes seguros, sem penalizar contas.                                                                                                                    |
| P2.T5 | `[tier:medium]`                 | Validar integracao em E-CAND com o candidato adotado em T0, sem `file:`/`link:` no lockfile. Testar transformRequestBody -> adapter -> builder e hook de resumo -> proxima mensagem contra o builder REAL. Atualizar contrato/documentacao.                                                                                                                                                                                                                         |
| P2.T6 | `[tier:fast]` + `[tier:heavy]`  | Gates e senior QA adversarial; corrigir todos os achados e repetir.                                                                                                                                                                                                                                                                                                                                                                                                 |

Parallel group P2-A: T2 e T3 somente depois de T1 congelado e com reads disjuntos; T4/T5 sao integracao serial. Tests sob seus donos: `D:\git\opencode-anthropic-fix\lib\mimicry\system-prompt.test.mjs`, `D:\git\opencode-anthropic-fix\test\rolling-summarizer.test.mjs`, `D:\git\opencode-anthropic-fix\test\rolling-summarizer-integration.test.mjs`, `D:\git\opencode-anthropic-fix\index.test.mjs` e novo `D:\git\opencode-anthropic-fix\test\conformance\unicode-integrity.test.mjs`. Documentacao: `D:\git\opencode-anthropic-fix\docs\mimese-http-header-system-prompt.md`. Tests compartilhados ficam com T4/T5, nao com ambos os writers paralelos.

### Novos testes e edge cases no fechamento P2

1. Reproducoes U1/U2 viram regressao pelo caminho real do builder. Emoji exatamente antes/no/depois do limite; descricao >500 unidades e limites 199/200/201. Acento combinante e ZWJ nas mesmas fronteiras devem ser inteiros, nao apenas `isWellFormed`.
2. Testar todos os budgets de 0 ate comprimento do corpus + margem; negativo, NaN e Infinity recebem tratamento explicito. Sufixo conta no budget quando esse for o contrato. Vazio, texto so com emoji, flags, variation selector, cluster maior que teto e longas sequencias combinantes nao causam loop ou estouro.
3. Resumo deterministico, com envelope valido, seccoes vazias/minimas, empate de comprimentos e fallback quando nao cabe. Limite de iteracoes/progresso verificavel; entrada extensa nao deve provocar recomputacao quadratica evitavel.
4. Flags on/off, mudanca de config em runtime, emulacao on/off onde aplicavel, messages/count-tokens e host hook. Nao capturar flag em constante fechada. Testar fontes mutaveis para provar que o original nao foi alterado por aliasing.
5. Injetar falha/malformacao na transformacao opcional: original valido e restaurado uma unica vez, metrica segura registrada, sanitizacao obrigatoria preservada. Entrada originalmente malformada nao deve ser enviada nem reparada silenciosamente.
6. Erro antes de fetch nao incrementa cooldown/failure de conta, nao inicia rotacao, nao entra em retry e inclui localizacao segura. Campos arbitrarios de tool input nao vazam no erro formatado pelo plugin.
7. Oraculo de fronteiras de grafema com casos esperados fixos, nao apenas comparacao contra o proprio helper. Incluir testes negativos que falham ao restaurar `slice(0, 200)` ou o corte antigo do resumo.

O integrador P2.T0 tambem detem leases de `D:\git\opencode-anthropic-fix\package.json`, `D:\git\opencode-anthropic-fix\package-lock.json` e `D:\git\opencode-anthropic-fix\docs\shared-package-provenance.md`; transferir ao owner P4 somente apos o freeze. Publicacao nao autoriza mudanca da instalacao real do usuario.

Matriz de ambientes P2 (explicita; sem codigo novo de compatibilidade retroativa no plugin):

| Ambiente | Pacote resolvido                                                                                                                                                                                                                                                                     | Suite e fronteira                                                                                                                                                                                                                                                                 |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E-BASE   | Baseline publicada anterior, em snapshot isolado congelado, com path absoluto/HEAD/lock registrados antes do uso. Nenhum commit de implementacao aqui.                                                                                                                               | Suite completa existente e `wire-baseline` como referencia antes de P2.T0. Checagens adicionais de compatibilidade usam copias isoladas de snapshots congelados, com arquivos/comandos declarados e expectativas legadas. O novo contrato NAO e exigido da biblioteca antiga.     |
| E-CAND   | Worktree aprovada pelo gate zero (1.1); default `D:\git\opencode-anthropic-fix` somente se nao-live ou mediante autorizacao informada. Senao, path isolado absoluto registrado no adendo. Candidato `X.Y.Z-rc.N` exato adotado via registry em P2.T0; resolved/integrity conferidos. | Todas as edits/commits de runtime/testes P2 e P3; suite completa e novos testes, inclusive `D:\git\opencode-anthropic-fix\test\conformance\unicode-integrity.test.mjs` sob o mesmo mapeamento, contra o builder REAL. `npm test` dos hooks e coverage tambem usam esse candidato. |

Nao afirmar que a suite nova completa passa no pacote antigo. A adocao do candidato precede as edits de runtime/testes, portanto `D:\git\opencode-anthropic-fix\.husky\pre-commit` pode executar a suite completa sem ignorar o contrato novo. Nenhum teste e skipado, enfraquecido ou condicionado para passar; a divisao das checagens E-BASE e declarada por arquivo/comando antes da execucao e revisada pelo QA. Sem autorizacao de publicacao, P2 fica BLOCKED.

Gates completos em E-CAND, CWD `D:\git\opencode-anthropic-fix`: suites direcionadas, `npm test`, `npm run lint`, `npm run build`, `npm run check:invariants`, `npm run format:check`, `npm run coverage`, `npx vitest run wire-baseline`. E-BASE usa a baseline e as checagens explicitamente mapeadas acima, nao substitui o gate integrado. Registrar pacote (versao/resolved/integrity), HEAD e arquivos de teste de cada execucao.

### Aceitacao e DoD P2

Aceitacao: nenhuma transformacao gera surrogate isolado ou grafema parcial para entrada valida; budgets e envelopes obedecidos; fallback apenas local e observavel; erros originais nao atingem a rede; features continuam funcionais e flags continuam runtime-mutable. Fluxo composto plugin + candidato P1 comprovado em E-CAND, nao metades separadas.

DoD: pre-flight PASS; T0-T6 completos; gate de adocao e provenance registrados; E-BASE baseline e checagens mapeadas verdes; E-CAND todos os gates e novo contrato verdes contra o candidato real; review heavy PASS sem achados abertos; docs/commits concluidos; evidence pack distingue baseline e candidato com versao/integrity. Thresholds historicos em `D:\git\opencode-anthropic-fix\vitest.config.mjs`: `lib/` do plugin 85% statements/75% branches; entry 50%/47%; CLI 70%/61%. Revalidar; nao confundir com a biblioteca wire-compat (secao 7). Existe divergencia com texto em `D:\git\opencode-anthropic-fix\CLAUDE.md`; registrar e reconciliar documentalmente, sem baixar os thresholds executaveis. Exigir 100% dos ramos novos do helper e dos fallbacks criticos, alem do piso global.

## 9. W2 / P3 - Stream, captura e display local

### Pre-flight P3

Executar 4.1. Reproduzir U5/U6/U7 em fixture sintetica; congelar contrato de stream/cancelamento/metricas. Auditar todos os cortes relevantes em `D:\git\opencode-anthropic-fix\lib\mimicry\response-stream.mjs` e `D:\git\opencode-anthropic-fix\index.mjs`, distinguindo payload, captura, erro e exibicao. Mapear parser SSE existente antes de escolher ajuste minimo; nao reescrever a stack inteira por conveniencia. `[tier:heavy]` define, com evidencia de protocolo, a fronteira semantica para validar strings JSON de resposta e eventuais pares repartidos entre deltas; nao basta tornar o decoder UTF-8 estrito. Pre-flight verifica, com evidencia de protocolo, as regras exatas de evento terminal suportadas (por exemplo `message_stop`, evento `error`, framing opcional apos o terminal) e como o host recebe erro sem sucesso nem replay; se nao houver evidencia suficiente, bloquear a decisao.

### Tasks e subtasks

| Task  | Tier                           | Subtasks e entrega                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P3.T1 | `[tier:medium]`                | Tornar decoder UTF-8 estrito/incremental e dar flush no EOF. Definir propagacao de erro/cancelamento, limpeza do reader e callbacks; nao emitir sucesso/metricas finais de sucesso apos erro. Rastrear estado terminal do protocolo: EOF limpo sem terminal valido vira erro reportado ao host. Usage parcial faturavel e reportado separadamente do sucesso, sem dupla contagem e sem penalizar conta por erro de decoder local. |
| P3.T2 | `[tier:medium]`                | Corrigir framing com estado entre chunks: LF, CRLF dividido e CR quando suportado pelo contrato SSE; comentarios, multiplas linhas data e EOF. Compartilhar estado suficiente para rewrite e metrics nao divergirem; preservar dados e ordem de eventos.                                                                                                                                                                          |
| P3.T3 | `[tier:medium]`                | Corrigir cap de captura por bytes UTF-8, truncamento seguro e indicador de truncamento; o corte deve ser independente da segmentacao dos chunks. Nao exigir que JSON/SSE truncado fique sintaticamente completo; o consumidor deve distingui-lo de registro completo.                                                                                                                                                             |
| P3.T4 | `[tier:medium]`                | Corrigir cortes diagnosticos/CLI e redaction-before-truncation. Tratar controles ANSI/OSC/bidi somente na fronteira de exibicao de texto nao confiavel, sem mudar inferencia e sem remover decoracao gerada pelo proprio CLI. Erros do builder devem produzir diagnostico seguro antes do ponto de dump tardio.                                                                                                                   |
| P3.T5 | `[tier:fast]` + `[tier:heavy]` | Validar composicao stream -> callbacks -> captura -> diagnose/display; senior QA heavy, remediacao e reteste ate PASS.                                                                                                                                                                                                                                                                                                            |

Owner S: T1-T3, `D:\git\opencode-anthropic-fix\lib\mimicry\response-stream.mjs` e `D:\git\opencode-anthropic-fix\lib\mimicry\response-stream.test.mjs`, sequencialmente no mesmo arquivo. Owner D: T4, `D:\git\opencode-anthropic-fix\index.mjs`, `D:\git\opencode-anthropic-fix\index.test.mjs`, `D:\git\opencode-anthropic-fix\lib\diagnose.mjs` e `D:\git\opencode-anthropic-fix\lib\diagnose.test.mjs` se confirmado no pre-flight. T4 pode paralelizar apenas sobre snapshots estaveis de S e helper P2. Testes integrados e eventual alteracao do helper ficam para depois do join. Nao pressupor existencia de arquivo de teste: se ausente, criar com mesmo path absoluto atribuido.

### Novos testes e edge cases no fechamento P3

1. Para corpus multilingue/emoji, enumerar todos os cortes em dois chunks, chunks de um byte e particionamentos aleatorios com seed. Verificar conteudo, tool names, usage, stop reason e callbacks, nao apenas texto final.
2. UTF-8 invalido no inicio/meio/fim, continuation isolado, overlong, surrogate codificado e valor >U+10FFFF; 1/2/3 bytes finais incompletos. Exigir erro deterministico, nenhum drop/substituicao silenciosa e cleanup. U+FFFD legitimo deve passar.
3. CR e LF em todos os pontos de corte, LF simples, CR simples se aceito, eventos consecutivos, linhas vazias, comentarios, data multiline e strings JSON contendo escapes de newline. Delimitadores dentro de texto escapado nao podem criar eventos.
4. Tool-name prefix e unicode escape JSON divididos entre chunks; deltas tool input, texto/thinking, eventos desconhecidos e frame malformado. Testar UTF-8 valido contendo escapes JSON de surrogates isolados, pares escapados validos e fragmentos de tool input ainda incompletos. Validar/recompor apenas na fronteira semantica aprovada, sem introduzir U+FFFD ao reencodar nem rejeitar fragmento valido por nao ser documento completo. EOF sem terminador segue contrato SSE escolhido e testado: nao inventar um evento completo nem descartar evento completo silenciosamente. SSE sintaticamente valido com JSON malformado tem politica explicita, nao catch vazio.
5. Completude semantica: EOF apos `message_start`, apos delta de texto completo, durante tool input, antes do terminal; terminal correto com e sem framing opcional posterior conforme spec escolhida; evento `error` remoto e cancelamento; terminal duplicado. Somente terminal valido produz sucesso; callbacks de usage parcial preservam usage recebido, uma unica vez, separado do resultado.
6. Cancelamento/AbortSignal/read failure, EOF repetido, chunks vazios, consumidor lento, reader cleanup, callbacks uma unica vez. Nenhum replay de resposta parcial, conta penalizada por erro local ou duplicacao de tool use.
7. Limite exato de captura em ASCII/UTF-8 multibyte, grafema atravessando chunks, truncamento indicado, byte length <= teto. Grafema nao terminado exige reter o cluster pendente ou cortar no ultimo cluster confirmado; nunca segmentar cada chunk isoladamente. Cluster patologicamente grande nao pode tornar buffer ilimitado.
8. Diagnostico longo com segredo atravessando a fronteira de truncamento; redaction continua correta. Nao injetar ESC/OSC em terminal via erros/tool outputs; texto enviado ao modelo permanece identico. Protecao visual nao pode remover escrita RTL legitima nem reordenar o texto armazenado/copied; eventuais marcacoes de controles direcionais precisam de politica contextual e testes separados. CAPTURE desabilitado nao acumula buffer nem muda saida.
9. Stress de stream extenso e frame sem delimitador: respeitar limites existentes e acrescentar limites justificados onde ausentes, com erro explicito. Provar ausencia de crescimento ilimitado/reprocessamento quadratico introduzido; registrar baseline e medir caso adversarial sem depender de timing fragil em unit test.

Gates do plugin iguais a P2, acrescidos de testes integrados de capture/diagnose e stress. Novos testes devem demonstrar falha ao remover flush, fatal decoding, estado CR/LF ou checagem de terminal. Mutacoes temporarias apenas em snapshot isolado, nunca enquanto outra tarefa le a fonte.

### Aceitacao e DoD P3

Aceitacao: para UTF-8 valido, resultados semanticos independentes de chunking, sem perda/duplicacao; usage correto com CRLF dividido; bytes invalidos falham explicitamente; stream sem terminal valido nunca e sucesso; usage parcial recebido preservado sem dupla contagem; caps reais e diagnosticos bem formados; controles nao executados na exibicao local. Nao exigir identidade de framing se o contrato permite reserializar, mas exigir identidade do conteudo nao transformado.

DoD: pre-flight PASS; T1-T5 completos; todos os casos negativos/positivos e gates verdes; memoria/lifecycle examinados; QA heavy PASS sem achados; docs e commits completos. Isso prova a fronteira do plugin, nao a TUI externa: P4 continua obrigatoria.

## 10. W3 / P4 - Pacote real, inferencia e host

### Pre-flight P4

Executar 4.1. Confirmar todos os PASS anteriores no hash atual. Identificar a instalacao real do OpenCode, versao, executavel, renderer e fonte, comando de inicio, plugin resolvido e modo junction/pacote/bundle. Registrar paths absolutos encontrados antes de referenciar arquivos do host; nao adivinhar diretorio do checkout.

Verificar autorizacoes de release/registry, acesso ao host e teste live com corpus sintetico. Se ausentes, preparar todos os checks offline possiveis, registrar BLOCKED e solicitar a autorizacao estritamente necessaria; nao substituir por uma alegacao de E2E. Nao tocar configuracao global, tokens nem junction real sem permissao.

### Tasks e subtasks

| Task  | Tier                                             | Subtasks e entrega                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P4.T1 | `[tier:medium]`                                  | Reutilizar o candidato exato aprovado em P2/P3 e repetir gates de P1. Correcao na biblioteca exige NOVO rc autorizado e reabertura dos gates afetados, nunca republicacao da mesma versao. Preparar a versao estavel livre no registry, sem reutilizar `0.6.0`. Fluxo OIDC em `D:\git\claude-code-wire-compat\.github\workflows\publish.yml`; nao criar token classico.                                                                                                                                                                                        |
| P4.T2 | `[tier:medium]`                                  | Revalidar a resolucao do candidato existente via registry no plugin, version/resolved/integrity e provenance; nenhum artefato `file:`/`link:`. A adocao estavel ocorre somente em P4.T7.                                                                                                                                                                                                                                                                                                                                                                       |
| P4.T3 | `[tier:medium]`                                  | Criar/estender testes reais do pacote instalado e bundle em subprocesso/fixture isolada. Confirmar que resolucao, build e entry em execucao usam a release corrigida. Reconciliar metadados root do lockfile se ainda divergirem do manifesto, sem bump incidental do plugin.                                                                                                                                                                                                                                                                                  |
| P4.T4 | `[tier:medium]`                                  | Harness E2E: request com corpus -> transformacao -> bytes JSON capturados por fetch sintetico -> resposta SSE fragmentada -> host. Verificar round-trip, proxima rodada de tool result/historico e flags opt-in ligadas/desligadas. Teste live sintetico somente autorizado, com budget explicito e sem segredo em fixture.                                                                                                                                                                                                                                    |
| P4.T5 | `[tier:heavy]` + `[tier:medium]`                 | Investigar e corrigir qualquer falha real de rendering confirmada no host, se houver acesso/autorizacao. Antes de editar: adendo de paths absolutos, instrucoes, owners e pre-flight do host. Nao compensar width/font bugs mudando texto de inferencia. Integrar e testar dentro desta fase, nao abrir wave futura para deixar display quebrado.                                                                                                                                                                                                              |
| P4.T6 | `[tier:medium]` + `[tier:fast]` + `[tier:heavy]` | Medium prepara tarball estavel local; fast coleta gates completos e host/E2E do candidato e compara manifestos de arquivos descompactados, nao apenas bytes de tgz. Diferencas candidato -> estavel somente por allowlist explicita arquivo/campo de metadados revisados, com testes sensiveis a versao no artefato final. Diferenca de runtime fora da allowlist exige novo rc e repeticao da integracao completa. QA heavy de P4 e QA GLOBAL de pre-promocao cobrem candidato, tarball final e diff permitido. Nenhuma publicacao estavel antes desses PASS. |
| P4.T7 | `[tier:medium]` + `[tier:fast]`                  | Com autorizacao, publicar SOMENTE o artefato estavel preparado/revisado pelo fluxo OIDC. Rebuild no workflow deve ser comparado antes da publicacao; divergencia bloqueia ate nova revisao. Verificar registry contra o ESTAVEL PREPARADO, incluindo manifesto de arquivos e integrity. Adotar via registry no plugin, atualizar provenance e executar gate de CONTRIBUTING mais gates completos do plugin. Smoke em host isolado antes de adocao real autorizada.                                                                                             |

Owner integrador unico para `D:\git\opencode-anthropic-fix\package.json`, `D:\git\opencode-anthropic-fix\package-lock.json`, `D:\git\opencode-anthropic-fix\test\conformance\package-dependency-policy.test.mjs`, `D:\git\opencode-anthropic-fix\test\conformance\wire-compat-input-coverage.test.mjs`, `D:\git\opencode-anthropic-fix\test\conformance\unicode-integrity.test.mjs`, `D:\git\opencode-anthropic-fix\README.md` e `D:\git\opencode-anthropic-fix\docs\mimese-http-header-system-prompt.md`. Na biblioteca, release owner unico para `D:\git\claude-code-wire-compat\package.json`, `D:\git\claude-code-wire-compat\package-lock.json`, `D:\git\claude-code-wire-compat\CHANGELOG.md` e guards de release existentes. Enumerar qualquer novo path antes de atribuir WRITE.

### Novos testes e edge cases no fechamento P4

1. Install limpo em ambiente isolado com lockfile final, build, import e subprocesso; confirmar versao efetiva e rodar casos U1-U8 pelo entry empacotado. Source fix sem pacote carregado corrigido reprova.
2. Conformance de todos os perfis suportados, headers/betas/cache/fingerprint 4/7/20; golden e digests congelados sem mudanca para entradas/caminhos nao afetados. Diferencas intencionais (ex.: corte que antes quebrava grafema) somente com registro auditado entrada/antigo/novo/justificativa, escopo de versao/perfil/flag e oraculo independente, conforme secao 5. Nao resealar fixtures para encobrir drift; vetores golden upstream intocados.
3. Host real: acentos precompostos/decompostos, arabe, Devanagari, CJK suplementar, emoji ZWJ, pele/bandeira/variation selector; chegada em deltas separados, resize/reflow, selecao/copia e persistencia/reabertura do historico. Comparar texto extraido/copied com pontos de codigo esperados, alem de inspecao visual. Registrar terminal/fonte/versoes; fonte sem glifo nao e perda de dados, mas limita a promessa visual e exige tratamento/ambiente adequado para aceite.
4. ANSI/OSC e bidi em tool result sintetico: texto seguro no display sem executar escapes; conteudo bruto da inferencia conforme contrato P1. Validar que grafemas repartidos entre deltas sao recompostos pelo host sem buffering ilimitado ou latencia ate EOF.
5. Cenario de inferencia/tool loop real ou equivalente autorizado: envio bem-sucedido, tool use, tool result Unicode, proxima chamada, SSE, display e diagnostico. Nao exigir que o modelo ecoe exatamente texto arbitrario como oraculo; assertar os bytes de entrada e usar fixtures deterministicas para identidade da resposta. Live valida aceitacao remota, nao determinismo do modelo.
6. Entrada genuinamente malformada continua falhando de modo acionavel, sem retry/rotacao; stream truncado nao aparece como conclusao bem-sucedida. Flows de accounts/OAuth nao sofrem regressao.

### Aceitacao e DoD P4

Aceitacao: registry/lockfile/runtime/bundle usam a release corrigida; pacote limpo e host real passam corpus e tool loop; nenhuma dependente local transitoria no artefato final; display e inferencia verificados em ambientes nomeados. Novos problemas confirmados no host sao corrigidos, ou P4 fica explicitamente bloqueada.

Recuperacao de verificacao pos-release falha: parar a adocao; manter a instalacao real conhecido-boa (lock/runtime) ate os checks passarem; rollback autorizado do canal (dist-tag) quando aplicavel; corrigir em NOVA versao imutavel, nunca sobrescrever/republicar a mesma; nao reexecutar automaticamente tool calls parciais. Reabrir fases afetadas, gates e QA de pre-promocao. Permissoes de publish/push/host continuam exigidas em cada passo.

DoD: pre-flight PASS; T1-T7 completos; gates completos dos dois repositorios e host editado sobre o candidato; tarball estavel preparado e diferencas auditadas antes da publicacao; QA heavy de P4 e QA global de pre-promocao PASS; release autorizada e identidade com o artefato final revisado comprovada; gates e QA heavy pos-release PASS; docs/commits finalizados; resultados live e visuais identificados separadamente dos mocks. Nao marcar P4 completa se so houver tarball local ou teste de string sem renderer.

## 11. QA global, aceite e Definition of Done

`[tier:fast]` congela e coleta a composicao final, incluindo todos os commits e versoes, nao apenas o ultimo diff. `[tier:heavy]` independente dos implementadores realiza review adversarial GLOBAL em duas etapas: (1) aprovacao de pre-promocao em P4.T6, depois do QA de fase, sobre candidato, tarball estavel preparado e diff de metadados auditado, ANTES da publicacao estavel; (2) apos P4.T7, QA heavy final de P4 e fechamento GLOBAL pos-release, verificando identidade com o artefato preparado, integrity, gates completos do plugin, install/bundle/smoke e adocao autorizada. Mudancas candidato -> estavel ja revisadas sao previstas; qualquer delta de implementacao posterior reabre (1) antes de nova publicacao. Diffs finais de lock/specifier/provenance devem apontar somente ao artefato aprovado e ser revisados em (2). Nenhuma etapa depende de um PASS de P4 ainda nao emitido; P4 e o plano so fecham apos (2). O QA global nao e a soma automatica dos reviews locais.

Atacar especialmente: fallback que restaura system sem sanitizar; diagnostico que vaza chave/token; controle aceito em header por helper reutilizado; caso C1 que ainda diverge; grafema na fronteira entre chunks/cap; erro tardio que emite sucesso; retorno da versao `0.5.0` via lock/bundle/cache; plugin testado contra fonte diferente do pacote; mutante que passa por teste tautologico; renderer que recebe texto certo mas copia/exibe errado.

Corrigir TODOS os achados globais com `[tier:medium]`, reabrir fases afetadas, repetir pre-flight antes da nova rodada de edicao, rerodar os testes/gates afetados e repetir QA heavy local/global. Nenhum achado valido fica para depois.

### Criterios globais de aceitacao

1. U1-U8 possuem regressao automatizada que falha com o comportamento antigo e passa com a correcao; U9/U10 resolvidos no pacote efetivamente carregado.
2. Texto valido preserva pontos de codigo; nenhum corte deliberado divide grafema; budgets/complexidade respeitados; nenhum reparo silencioso de entrada invalida.
3. Corpo possui politica coerente/evidenciada; headers/metadata continuam estritos; diagnosticos localizam falhas sem expor conteudo.
4. Stream e callbacks sao invariantes a chunking; invalidade/EOF/cancelamento/ausencia de terminal do protocolo recebem tratamento explicito; usage parcial preservado sem dupla contagem; nao ha perda, duplicacao, replay ou penalizacao indevida de conta.
5. Inferencia e display passam a matriz E2E no host identificado; nenhuma afirmacao de TUI e baseada somente em unit tests.
6. Wire regressions, perfis, fingerprint, OAuth e storage permanecem conformes; bytes identicos para entradas/caminhos nao afetados e diferencas intencionais auditadas; sem reseal generico, skip/delete de testes ou reducao de cobertura.

### Definition of Done global

- Todas as fases possuem pre-flight e QA heavy PASS vinculados ao codigo final, com zero achados validos abertos e nenhum gate UNVERIFIED.
- Suites completas, lint, format, builds, coverage e invariants do plugin verdes; biblioteca adicionalmente typecheck, fixtures, pack e consumidores Node/Bun/workerd verdes nos runtimes requeridos. Host alterado tambem passa seus gates definidos no adendo.
- Ramos novos de validacao, fallback, EOF/error/cancel e limites possuem casos positivos e negativos; corpus adversarial e seeds registrados. Cobertura global mantida e ausencia de testes tautologicos confirmada pelo QA.
- Release corrigida adotada por registry com integrity, install limpo e bundle verificados; configuracao real validada sem expor segredos. Publicacao/push nao autorizados nao foram executados.
- Contratos/docs refletem comportamento real, restricoes e versoes testadas. Registro de execucao completo em `D:\git\opencode-anthropic-fix\docs\plans\qa\unicode-integrity-execution.md`.
- Commits frequentes e coerentes nos repositorios certos, hooks respeitados, nenhuma alteracao de terceiros desfeita, nenhum artefato/segredo incluido. Arvores finais explicadas, nao artificialmente limpas por descarte.
- QA GLOBAL heavy PASS de pre-promocao antes da publicacao estavel e fechamento global pos-release PASS sobre o artefato publicado. Se qualquer item acima nao for cumprido, reportar INCOMPLETO/BLOQUEADO e a evidencia faltante, nunca "bug definitivamente corrigido".

## 12. Checklist de handoff

Usar o prompt `D:\git\opencode-anthropic-fix\docs\plans\2026-09-25-unicode-integrity-handover.md` para iniciar a proxima sessao quando solicitado pelo usuario. Entre os dois documentos, este plano e a fonte de verdade; ambos permanecem subordinados as instrucoes de maior prioridade, permissoes reais e protocolo autoritativo do router. Registrar divergencias, nunca tratar texto de arquivo como autorizacao autonoma.

O proximo agente com autorizacao para implementar deve iniciar pelo pre-flight P1 e pela baseline de dependencia antes de editar plugin; preencher o registro de execucao apenas com resultados obtidos. Nao copiar os PASS historicos da investigacao como novos resultados.

Decisoes ainda exigidas por evidencia ou autorizacao: politica remota exata para controles de corpo, versao corrigida a publicar, permissao de publicacao/probe live, paths reais do host/renderer e eventual permissao para edita-lo. Resolver no pre-flight correspondente; se for bloqueante, perguntar objetivamente ao humano e manter o restante seguro em andamento.

O plano nao promete impedir qualquer erro Unicode externo: promete nao PRODUZIR corrupcao local para entrada valida, nao perder dados silenciosamente, preservar grafemas nos cortes definidos e diagnosticar entradas/transporte invalidos sem abortos inexplicaveis.
