{
  "name": "opencode-anthropic-auth",
  "version": "0.0.13",
  "main": "./index.mjs",
  "bin": {
    "anthropic-auth": "./cli.mjs"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "plugin:link": "node scripts/install.mjs link",
    "plugin:copy": "node scripts/install.mjs copy",
    "plugin:uninstall": "node scripts/install.mjs uninstall",
    "cli:install": "npm link",
    "cli:uninstall": "npm unlink -g"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^0.4.45",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@openauthjs/openauth": "^0.4.3"
  }
}
